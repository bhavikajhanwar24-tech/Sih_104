import { AnalystConsole } from '@/views/AnalystConsole.jsx';

/**
 * App shell — P3.2 end-to-end milestone (mic → ml-engine → Java → STOMP → gauge).
 */
export default function App() {
  return <AnalystConsole sessionId="browser-dev" />;
}
