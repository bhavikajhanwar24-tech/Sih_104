package com.sentinelvoice.service;

/**
 * @deprecated Replaced by {@link com.sentinelvoice.challenge.ChallengeService} (P8.4).
 * Retained as an empty marker so old imports fail loudly at compile time if misused.
 * Do not use — client-supplied timestamps made this control meaningless (defect #6).
 */
@Deprecated(forRemoval = true)
public final class ChallengeResponseService {
    private ChallengeResponseService() {
    }
}
