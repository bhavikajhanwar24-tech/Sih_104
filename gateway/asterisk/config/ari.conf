; =============================================================================
; Asterisk REST Interface — used by P7.3 ActuationService (hold / terminate)
; DEMO CREDENTIALS ONLY — lab PBX, not production.
; =============================================================================

[general]
enabled = yes
pretty = yes
allowed_origins = http://127.0.0.1:*,http://localhost:*

[sentinel]
type = user
read_only = no
; DEMO PASSWORD — lab only (matches README / test-sip-call.sh)
password = sentineldemo
