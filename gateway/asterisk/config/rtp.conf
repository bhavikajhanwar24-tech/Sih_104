; RTP media ports (document these if a host firewall is enabled)
[general]
; Narrow range — Docker Desktop struggles mapping 100+ UDP ports.
rtpstart=10000
rtpend=10020
icesupport=no
; Off for Docker Desktop NAT: first RTP often arrives from a rewritten source.
strictrtp=no
