# F0 — Reset project rules, V2 architecture, version contracts

## What was built

Documentation and repository hygiene only — **no runtime behaviour change**.

1. Rewrote `.cursor/rules/sentinelvoice.mdc` for SentinelVoice V2 (multi-tenant, policy-aware,
   PostgreSQL, Flyway-only, deep refactors allowed, tests deferred to F20).
2. Added `docs/v2/ARCHITECTURE.md` (planes diagram, onboarding + live-call sequences, latency
   budget, trust boundaries, threat model).
3. Added `docs/v2/CHANGELOG.md` with a Deviations from v1 section.
4. Moved historical schemas to `docs/contracts/v1/`; created `docs/contracts/v2/` copies with
   required `tenantId` (uuid) and `schemaVersion` const `"2"` on every message schema.
5. Pointed `scripts/gen-contracts.sh` and `scripts/gen-validators.mjs` at v2.
6. Strengthened `.gitignore`; replaced absolute machine paths in `docs/EVALUATION_REPORT.md`.
7. Updated root `README.md` for V2 and linked `docs/v2/`.

## API endpoints

None. F0 is docs/hygiene only.

## DB tables touched

None. No Flyway migrations in F0.

## Env vars

None introduced.

## How to run

No services required. Inspect docs:

- `docs/v2/ARCHITECTURE.md`
- `docs/v2/CHANGELOG.md`
- `docs/contracts/v2/README.md`
- `.cursor/rules/sentinelvoice.mdc`

Optional (expected to fail until later features update producers):

```bash
bash scripts/gen-contracts.sh
```

## Manual verification script

1. Open `.cursor/rules/sentinelvoice.mdc` — confirm it describes V2 multi-tenant rules and does
   **not** contain “contracts are FROZEN”, “small reviewable diffs”, or “STOP and ask”.
2. Open `docs/v2/ARCHITECTURE.md` — confirm mermaid plane diagram, two sequence diagrams,
   latency table, trust boundaries, and threat model are present.
3. Confirm `docs/contracts/v1/FeatureFrame.schema.json` exists (historical) and
   `docs/contracts/v2/FeatureFrame.schema.json` lists `tenantId` and `schemaVersion` in
   `required`, with `schemaVersion.const === "2"`.
4. Confirm `scripts/gen-validators.mjs` sets `contractsDir` to `docs/contracts/v2`.
5. Confirm `docs/EVALUATION_REPORT.md` has no `C:/Users/...` absolute paths.
6. Confirm root `README.md` links to `docs/v2/` and marks quickstart as filled by F19.
